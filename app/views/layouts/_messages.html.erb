<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %>">
    <span class="alert-icon"></span>
    <a id="close-flash" href="#" class="close"><span class="icon-cross"></span></a>
      <ul>
        <li>
          <%= value %>
        </li>
      </ul>
  </div>
<% end %>