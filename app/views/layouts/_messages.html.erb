<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %>">
    <a id="close-flash" href="#" class="close">×</a>
      <ul>
        <li>
          <%= value %>
        </li>
      </ul>
  </div>
<% end %>