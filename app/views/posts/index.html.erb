<!--
<h3> All Posts </h3>

<div>
  <ul>
    <% #@posts.each do |post| %>
      <li>
      <h1><%= link_to markdown(post.title), post_path(post) %></h1>
      <p><%= #truncate(markdown(post.body), length: 100, escape: false) %></p>

      </li>
    <% #end %>
  </ul>
</div>
 -->


